<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hotdog.user.dao.UserManagementDao">

	<insert id="insertUser" parameterType="userManagementVO">
		<selectKey keyProperty="userSeq" resultType="string" order="BEFORE">
			SELECT COALESCE(MAX(USER_SEQ), '0') + 1 FROM USER_MNG
		</selectKey>
		INSERT INTO USER_MNG
		(USER_SEQ, USER_ID, USER_NAME, PASSWD, EMAIL_ADDR, GENDER, RGST_DATE, RGST_USER_ID, UPDT_DATE, UPDT_USER_ID, DELT_YN)
		VALUES
		(
			#{userSeq}, 
			#{userId},
			#{userName},
			#{password},
			#{emailAddr},
			#{gender},
			SYSDATE,
			'ADMIN',
			NULL,
			NULL,
			'N'
		)
		
	</insert>
</mapper>