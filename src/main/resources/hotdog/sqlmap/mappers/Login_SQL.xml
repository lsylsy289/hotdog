<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hotdog.login.dao.LoginDao">

	<select id="selectConfirm" parameterType="string" resultType="egovMap">
		SELECT	/* sqlMapId(LoginDao.selectConfirm) */
				A1.USER_ID,
				A1.USER_NAME,
				A1.EMAIL_ADDR,
				A1.GENDER,
				A1.PASSWD	AS PASSWORD
		FROM	USER_MNG	A1
		WHERE	A1.USER_ID	= #{userId}
	</select>
</mapper>